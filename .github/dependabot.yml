# Keeps the pinned GitHub Actions up to date (Bashou itself has no Python dependencies).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
